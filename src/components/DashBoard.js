import React from 'react';
import Calendar from './Calendar';

const DashBoard = (props) => {
    return (
      <div>
      <h4>Select Date:</h4>
      <Calendar />
    </div>
    );
  };

export default DashBoard;